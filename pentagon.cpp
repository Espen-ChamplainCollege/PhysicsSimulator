#define _USE_MATH_DEFINES

#include "pentagon.h"
#include <cmath>

pentagon::pentagon(Point center, double radius, Color color) {
    //double points = [0, 1, sin((2 * M_PI) / 5), cos((2 * M_PI) / 5)];
}
